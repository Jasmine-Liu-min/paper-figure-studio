# 论文配图工作台 (Paper Figure Studio) · 项目介绍

> 一句话:把"想清楚了但画不出来"变成"几分钟出一版能用的图"。

## 📌 这是什么？

「**论文配图工作台 (Paper Figure Studio)**」是一个基于 AI 的科研配图生成工具,把论文 / 研究内容**一键转成能直接用的图**——论文思维导图、引言架构图、出版级数据图表代码,可编辑、可导出。

## 💡 为什么做这件事？

写论文、做组会汇报的时候,**画图常常比写内容还费时间**:

- 手动用 draw.io / PPT 画架构图,半天画不好还不好看;
- 直接丢给 AI 文生图,框图里的文字全是乱码,根本不能放进论文;
- 想做规范的统计图,又得现学 matplotlib / ggplot 调半天样式。

说白了就是"**内容早想清楚了,卡在画图上**"。所以做了这套工具,解决三件事:**画得快、标签画得准、配色统一好看**。

## 👤 面向什么用户？

- 写论文 / 投稿、做课程项目和组会汇报的**学生与科研人员**;
- 尤其是**统计 / 机器学习 / 搜索推荐 / 图像处理**方向(内置对应场景预设);
- 不熟绘图工具(draw.io / matplotlib / ggplot)、但需要规范配图的人。

## ⚙️ 能做什么？

1. **读论文 → 思维导图**:上传 PDF(MinerU 高精度解析,中文/公式/双栏都能处理)或粘贴文本,自动生成 ChatPaper2Xmind 风格的结构大纲树;导出 PNG / PDF / SVG / Markdown / Mermaid / XMind。
2. **讲项目 → 架构图**:把方法描述变成引言用的全文架构图——LLM 抽结构 + Dagre 自动分层布局(自动处理双分支、反馈回环、避免连线交叉),**纯矢量绘制,标签 100% 正确、配色统一**;导出 SVG / PNG / PDF,直接插论文。
3. **出数据 → 图表代码**:粘上数据,生成**出版级 seaborn / ggplot2 绘图代码**(图例外置、300dpi、同时导 PNG+PDF),本地一键跑出图。

## 🎯 能带来什么？

- **对写论文的同学**:架构图、思维导图、数据图一站式搞定,省下大把画图和调样式的时间。
- **对赶 deadline 的人**:标签不会画错、配色自动统一,出来就是能直接放进论文/汇报的图。
- **对不熟绘图库的人**:数据图直接给可运行代码,不用从零学 matplotlib / ggplot。

## 🧠 设计取舍(为什么这么做)

- **架构图坚持矢量(SVG),不用图片模型**:论文图的标签必须 100% 正确,而扩散类文生图模型画不准密集英文文字。矢量渲染保证标签精确、可编辑、可无损导出。
- **数据图用代码,不用图片模型**:数据图必须按真实数据画(图片模型会编造数值),所以生成 seaborn/ggplot2 代码,数据准确且可复现。
- **AI 配图作为可选项**:想要"插画质感"时可接图片模型(见 README),但默认关闭,避免文字出错误导。

> 技术细节、部署与 API 配置见 [README.md](./README.md)。
